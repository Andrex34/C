#include <stdio.h>

int main() {
    char balls[3][6] = {"red", "green", "blue"};

    printf("Массив шаров:\n");
    for (int i = 0; i < 3; i++) {
        printf("ball %s\n", balls[i]);
    }
    return 0;
}
