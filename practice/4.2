#include <stdio.h>

int main() {
    int number = 5;
    int blue_balls[number];
    for (int i = 0; i < number; i++) {
        blue_balls[i] = 1;
    }
    int size = sizeof(blue_balls) / sizeof(blue_balls[0]);
    printf("Количество шариков в коробке: %d\n", size);

    return 0;
}
