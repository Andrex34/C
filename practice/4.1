#include <stdio.h>

int main() {
    int number = 10;
    int red_balls[number];
    for (int i = 0; i < number; i++) {
        red_balls[i] = 1;
    }
    int size = sizeof(red_balls) / sizeof(red_balls[0]);
    printf("Количество шариков в коробке: %d\n", size);

    return 0;
}
