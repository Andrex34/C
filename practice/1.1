#include <stdio.h>

int main() {
    float c, f;
    
    printf("Введите температуру в градусах Цельсия: ");
    scanf("%f", &c);

    f = (c * 9 / 5) + 32;

    printf("Температура в Цельсиях = %.1f ; Температура в Фаренгейтах = %.2f", c, f);
    
return 0;
}
